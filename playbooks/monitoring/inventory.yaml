# Monitoring host: Grafana, VictoriaMetrics and friends.
# Note the login is kernelci here, not azureuser like the other hosts.
all:
  hosts:
    mon:
      ansible_host: 20.168.1.249
      ansible_port: 22
      ansible_user: kernelci
      ansible_become: true
